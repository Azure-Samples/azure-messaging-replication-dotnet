## Message Replication Task Functions

This folder contains two sets of Message Replication Task Functions.

* **[config](config)** - Configuration-only projects, which use the standard replication task library
* **[code](code)** - .NET Core Azure Functions projects as starting points for your custom replication tasks that require modification of events or messages as they are being moved
